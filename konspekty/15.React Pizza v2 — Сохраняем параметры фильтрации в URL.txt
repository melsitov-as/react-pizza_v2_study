Сохранение параметров в url и переход на нужную страницу лишь по одной ссылке с параметрами

Вшить параметры в адресную строчку
1. Установить библиотеку qs
2. Импортировать
3. Создать отдельный useEffect, который будет отвечать за парсинг параметров и вшивания их в адресную строчку

useNavigate

когда параметры вшиты их необходимо спарсить и дальше их обрабатывать из url и передавать в redux
до получения пицц будет проверять есть ли у нас в url эти параметры

useSearchParams - вытащить параметры из url
window.location.search

useEffect(() => {}, []); - будет происходить при первом рендере

Кто делал все как в видео, но у него не работает, попробуйте убрать React.StrictMode в index.js. Он два раза вызывает useEffect, и получается второй вызов перетирает наш params.