коммит Добавляет логику отображения всех пицц и сортировку

1. Статичные файлы, иконки, картинки если в реакте это нужно как отобразить, где-то применить или что-то сделать есть 2 способа:
1й-способ создать папку в public -> img и перекинуть туда картинки в хедере там где рендерится картинка вместо того, чтобы писать ./img написать img/photo.jpeg

2-й способ - сделать через import вместо того, чтобы передавать в public папку с картинками сделать
src->img и теперь уже в компоненте из папки src импортировать картинку import logoSvg from '../img/pizza-logo.svg' и в компоненте src={logoSvg}

В реакте когда создаем статичные файлы и хотим их импортировать, принято принято писать папку assets src->assets->img

2. Сделаем, чтобы категории выделялись при клике на категорию

3. Рендеринг списка
Сокращать код и делать переиспользуемым

<></> - родительский элемент без div'a

если анонимная функция выполняет только одну часть кода, одну функцию, то можно не создавать вот это 
const onClickCategory = (index) => {
		setActiveIndex(index);
	} 

мы можем вместо передачи конкретно каждого свойства если мы знаем что в объекте будут точно такие же параметры как они будут в компоненте
<PizzaBlock {...obj}/> spread-оператор
библиотека nanoid
нельзя передавать в key index если массив меняется и можно если массив не меняется


