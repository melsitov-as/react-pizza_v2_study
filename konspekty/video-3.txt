1. используем плагин prettier
prettier позволяет форматировать код
установили prettier в расширения vs code
настройка prettier
ctrl+shift+p -> settings -> preferences -> open settings ->user settings json -> копировать весь код везде где написано prettier -> "editor.formatOnSave"

при сохранении файла prettier выравнивает код нужно донастроить:


2. преобразовываем верстку в реакт-проект
выделить и нажать ctrl+d несколько раз чтобы выбрать выделенное одинаковое

3. css
установка scss
установить библиотеку sass

npm install sass

npx - установка и запуск js программ с автоматическим удалением после этого
npm - установка библиотек в проект

! в конце изменить название, что было сделано и добавить описание коммита

ссылку на шрифты вставляем в index.html в public

4. логотип

5. разбиваем на компоненты
в реакте в одном компоненте не сохраняют много html-кода
разбиваем приложение на отдельные компоненты:
- header
- categories
- sort
- pizza-card

Названия компонентов в реакте с большой буквы
Отобразить компонент
<Categories/>
{Categories()}

6. пропсы
передать пропс в виде строки price="500"
передать пропс в виде числа price={500} - так можно передавать любой тип данных который есть в js
