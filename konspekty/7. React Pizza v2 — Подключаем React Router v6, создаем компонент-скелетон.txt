1. Skeleton
Сделать флаг, который будет говорить, что пиццы загрузились
Получение данных с сервера

2. Роутинг React Router - нужна, чтобы делать навигацию по сайту и при этом без перезагрузки всей страницы
в index.js роутинг импортируем в index.js

BrowserRouter - обернуть на App.js

- главная страница
- корзина
- ничего не найдено

в src создать папку pages и в этой папке будет компонент Home.jsx
Будем писать логику где содержимое будет в отдельных jsx-файлах, но хэдер будет всегда везде один

css-модули


Что сделано в этом видео, если react-router-dom не будет работать сделать самому

NotFoundBlock.module.scss

На все приложение в index.js прикручиваем логику, а там где мы хотим там где нам надо мы юзаем <Routes></Routes>

Благодаря <Link></Link> - моментальный переход

root.render(
	<React.StrictMode>
		<BrowserRouter>
			<App />
		</BrowserRouter>
	</React.StrictMode>
); - все приложение знает о реакт-роутере