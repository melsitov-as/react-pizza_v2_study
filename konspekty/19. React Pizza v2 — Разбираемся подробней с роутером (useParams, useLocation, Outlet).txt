
The object notation for `createSlice.extraReducers` is deprecated, and will be removed in RTK 2.0. Please use the 'builder callback' notation instead: https://redux-toolkit.js.org/api/createSlice


Когда буду кликать на любую пиццу - будет отдельная страница для каждой пиццы

Хуки в библиотеке react-router

<Outlet/>

useLocation - получает url и перерисовывает компонент к которому прикручен при изменении url location

хуки нужны, чтобы твой компонент, который следит за изменением пути параметров чтобы хуки заставили этот компонент сделать перерисовку при изменении параметров, если мы будем использовать useParams или же при изменении location если мы будем использовать useLocation

скрывать кнопку с ценой, если перешли в корзину


useParams - возвращает объект с динамическими параметрами из url, чтобы оповестить компонент о том, что нужно сделать перерисовку и вернуть в переменную те динамические параметры, которые мы будем передавать в url если они будут в url содержаться

параметры после ? - window.location или useSearchParams() 
внутри useLocation свойство search

<Outlet/>
Контент может быть разным а меню и хэдер всегда один
Шаблонный контент и если  между ними будет какой-то динамический контент передать просто компонент <Outlet/>
Когда будешь создавать главный <Route/> в нем укажи компонент-шаблон в котором будет хэдер шаблон и какие-то другие роуты в эти роуты сообщи

Компонет, а все что внутри него будет рендериться внутри компонента
Outlet следует использовать в родительских элементах маршрутов для отображения их дочерних элементов маршрутов это позволить отображать вложенный пользовательский инотерфейс при отображении дочерних маршрутов если родительский маршрут точно соответствует он будет отображать дочерний индексный маршрут или ничего, если индексного маршрута нет

если пицца не найдется, то перенаправлять пользователя на главную